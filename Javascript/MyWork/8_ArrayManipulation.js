const numbers = [25, 10, 35, 18, 42, 5];
const maxNumber = Math.max(...numbers);
const minNumber = Math.min(...numbers);
console.log("Maximum Number:", maxNumber);
console.log("Minimum Number:", minNumber);